# Python-зависимости бэкенда (FastAPI + LangGraph-пайплайн).
# Для Docker и `pip install -r requirements.txt`. Версии — нижние границы.
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
openai>=1.40.0
pydantic>=2.7.0
python-dotenv>=1.0.0
fastapi>=0.110
uvicorn[standard]>=0.29
httpx>=0.27
# Опционально: Claude по подписке (боты 1-4 + редактор без оплаты токенов).
# Требует логина Claude Code (credentials) или OAuth-токена. Без подписки
# боты идут через OpenRouter — пакет не мешает, импорт ленивый.
claude-agent-sdk>=0.2.0
